set timeout=0
set default=0

menuentry "ICDA" {
    insmod all_video
    insmod gfxterm
    insmod part_gpt
    insmod fat
    insmod search
    insmod search_fs_file
    search --file --set=root /boot/kernel.bin
    multiboot2 /boot/kernel.bin
    boot
}
